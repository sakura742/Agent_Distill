[
  {
    "name": "search_civil_law",
    "description": "检索中国民法典相关法律条款。",
    "domain": "civil",
    "collection": "civil_law",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "法律问题、关键词或案情摘要"},
        "limit": {"type": "integer", "description": "返回结果数量", "default": 3, "minimum": 1, "maximum": 10}
      },
      "required": ["query"]
    }
  },
  {
    "name": "search_labor_law",
    "description": "检索劳动法及劳动合同法相关法律条款。",
    "domain": "labor",
    "collection": "labor_law",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {"type": "string", "description": "法律问题、关键词或案情摘要"},
        "limit": {"type": "integer", "description": "返回结果数量", "default": 3, "minimum": 1, "maximum": 10}
      },
      "required": ["query"]
    }
  }
]
